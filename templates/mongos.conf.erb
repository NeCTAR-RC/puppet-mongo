# mongos.conf - generated from Puppet

# Where to log
logpath=<%= @logpath %>
logappend=<%= @logappend %>

# Connection string
# e.g. configdb=mongos1:27017,mongos2:27017,mongos3:27017
configdb=<%= @config_hosts.to_a.flatten.map{ |x| x + ':' + @config_port}.join(',') %>
